package com.socio.socio.entity;

/**
 * Role is an enum representing the different user roles within the application.
 * It helps to define access control and permissions for users.
 */

public enum Role {
    USER,
    ADMIN
}