spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_service_db
    username: root
    password: 9274@Indian
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081